image: r-base

before_script:
  - Rscript requirements.r

test:
  script:
    - cd src && Rscript Tests.r

pages:
	script:
		- mkdir .public
		- cp -r * .public
		- mv .public public
	artifacts:
		paths:
			- public
	only:
		- master
